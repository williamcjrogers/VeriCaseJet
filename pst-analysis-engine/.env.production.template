# VeriCase PST Analysis - Production Environment (AWS)
# INSTRUCTIONS: Fill in the REPLACE_WITH_* values below

# AWS mode flag
USE_AWS_SERVICES=true

# AWS S3 settings (already configured)
AWS_STORAGE_BUCKET_NAME=vericase-data
S3_BUCKET_NAME=vericase-data
AWS_S3_REGION_NAME=eu-west-2
AWS_REGION=eu-west-2

# Database - AWS RDS PostgreSQL (already configured)
DATABASE_URL=postgresql://VericaseDocsAdmin:Sunnyday8?!@database-1.cv8uwu0uqr7f.eu-west-2.rds.amazonaws.com:5432/postgres

# OpenSearch - OPTIONAL (can comment out to skip)
# Create at: https://console.aws.amazon.com/aos/
# OPENSEARCH_HOST=REPLACE_WITH_OPENSEARCH_ENDPOINT
# OPENSEARCH_PORT=443
# OPENSEARCH_USE_SSL=true
# OPENSEARCH_VERIFY_CERTS=true
# OPENSEARCH_INDEX=emails

# Redis - REQUIRED
# Create ElastiCache at: https://console.aws.amazon.com/elasticache/
REDIS_URL=REPLACE_WITH_REDIS_ENDPOINT
CELERY_QUEUE=pst-processing

# Tika - OPTIONAL (can comment out to skip)
# Deploy apache/tika:latest-full to ECS or skip
# TIKA_URL=REPLACE_WITH_TIKA_URL

# API settings
API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=REPLACE_WITH_YOUR_DOMAIN
JWT_SECRET=Nbsm9EdJViCtLkGrAWS3eQu7vPZjUgacKYBF5fy81RzoIOhMTw6plHxq04n2DX
JWT_ISSUER=vericase-pst-analysis
JWT_EXPIRE_MIN=7200

# Admin account
ADMIN_EMAIL=admin@vericase.com
ADMIN_PASSWORD=REPLACE_WITH_SECURE_PASSWORD

# Email settings (for notifications)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_FROM=noreply@vericase.com
EMAIL_HOST_USER=REPLACE_WITH_YOUR_EMAIL
EMAIL_HOST_PASSWORD=REPLACE_WITH_APP_PASSWORD

# AI Model API Keys - ROTATE THESE IMMEDIATELY!
# Get new keys from:
# - Claude: https://console.anthropic.com/settings/keys
# - OpenAI: https://platform.openai.com/api-keys
# - Gemini: https://aistudio.google.com/app/apikey
# - Grok: https://console.x.ai/
# - Perplexity: https://www.perplexity.ai/settings/api
GEMINI_API_KEY=REPLACE_WITH_NEW_KEY
CLAUDE_API_KEY=REPLACE_WITH_NEW_KEY
OPENAI_API_KEY=REPLACE_WITH_NEW_KEY
GROK_API_KEY=REPLACE_WITH_NEW_KEY
PERPLEXITY_API_KEY=REPLACE_WITH_NEW_KEY

# AI Feature Flags
ENABLE_AI_AUTO_CLASSIFY=true
ENABLE_AI_DATASET_INSIGHTS=true
ENABLE_AI_NATURAL_LANGUAGE_QUERY=false
AI_DEFAULT_MODEL=gemini

# Python settings
PYTHONUNBUFFERED=1
DEV_NO_AUTH=false
