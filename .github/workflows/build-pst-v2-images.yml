name: Build PST V2 Images

on:
  push:
    branches: [main]
    paths:
      - "services/pst-extractor/**"
      - "services/pst-loader/**"
      - ".github/workflows/build-pst-v2-images.yml"
  workflow_dispatch:

concurrency:
  group: build-pst-v2-images-main
  cancel-in-progress: true

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    env:
      AWS_REGION: eu-west-2
      AWS_ACCOUNT_ID: "526015377510"
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: pst-extractor
            context: ./services/pst-extractor
            dockerfile: ./services/pst-extractor/Dockerfile
            ecr_image: 526015377510.dkr.ecr.eu-west-2.amazonaws.com/vericase-pst-extractor
            ghcr_image: ghcr.io/${{ github.repository_owner }}/vericase-pst-extractor
          - name: pst-loader
            context: ./services/pst-loader
            dockerfile: ./services/pst-loader/Dockerfile
            ecr_image: 526015377510.dkr.ecr.eu-west-2.amazonaws.com/vericase-pst-loader
            ghcr_image: ghcr.io/${{ github.repository_owner }}/vericase-pst-loader

    steps:
      - uses: actions/checkout@v4

      - name: Preflight checks (required secrets)
        shell: bash
        run: |
          set -euo pipefail
          if [ -z "${{ secrets.AWS_ROLE_TO_ASSUME }}" ]; then
            echo "Missing required GitHub Secret: AWS_ROLE_TO_ASSUME" >&2
            exit 1
          fi
          echo "Preflight OK"

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v3

      - name: Configure AWS Credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          role-session-name: GitHubActions-${{ github.run_id }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Log in to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ matrix.ecr_image }}
            ${{ matrix.ghcr_image }}
          tags: |
            type=raw,value=latest
            type=raw,value=${{ github.sha }}
            type=raw,value={{date 'YYYYMMDD-HHmmss'}}

      - name: Build and push
        id: build
        uses: docker/build-push-action@v5
        with:
          builder: ${{ steps.buildx.outputs.name }}
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          platforms: linux/amd64
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Summary
        shell: bash
        run: |
          {
            echo "## 📦 PST V2 Image Published"
            echo ""
            echo "**Service:** \`${{ matrix.name }}\`"
            echo ""
            echo "**ECR:** \`${{ matrix.ecr_image }}:${{ github.sha }}\`"
            echo "**GHCR:** \`${{ matrix.ghcr_image }}:${{ github.sha }}\`"
            echo ""
            echo "**Digest:** \`${{ steps.build.outputs.digest }}\`"
          } >> "$GITHUB_STEP_SUMMARY"


