name: Deploy to EC2

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger EC2 deployment
        run: |
          echo "✅ Code pushed to main branch"
          echo "📦 Docker image will be built automatically"
          echo "🚀 Pull latest image on EC2 with: docker-compose pull && docker-compose up -d"
