name: Deploy to EC2

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment notification
        run: |
          echo "✅ Code pushed to GitHub"
          echo "📦 Docker images building automatically"
          echo ""
          echo "To deploy on EC2, run:"
          echo "ssh ec2-user@35.179.167.235 'cd ~/vericase && git pull && docker-compose pull && docker-compose up -d'"
